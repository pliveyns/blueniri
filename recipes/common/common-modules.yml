modules:

  - type: files
    files:
      - source: system
        destination: /

  - type: script
    snippets:
      #- "dnf5 install -y 'dnf5-command(copr)'"
      - "dnf5 install -y 'dnf5-command(config-manager)'"

  # Universal Blue packages
  - type: dnf
    repos:
      copr:
        - ublue-os/packages
    install:
      packages:
        - uupd
        - ublue-setup-services
        - ublue-os-udev-rules
        - ublue-os-signing
        - ublue-os-luks
        - ublue-os-just

  - type: dnf
    install:
      packages:
        - glibc-langpack-en
        - langpacks-en_GB
        - distrobox
        - alacritty
        - kitty
        - kitty-terminfo
        - syncthing
        - rclone
        - fzf
        - just
        - powerstat
        - git
        - wget
        - NetworkManager-wifi
        - Thunar
        - thunar-archive-plugin
        - thunar-volman
        - nautilus
        - libglvnd-gles
        - cifs-utils
        - adw-gtk3-theme
        - xdg-utils
        
    remove:
      packages:
        - firefox
        - firefox-langpacks

  - type: script
    scripts:
      - bootc-update-policy.sh
      - bootc-sudo-access.sh

